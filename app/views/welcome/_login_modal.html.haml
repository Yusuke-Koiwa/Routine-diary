= icon('fa', 'times', class: "close-btn")
.form__title
  .form__title--active
    Log in
  #signup-link.form__title--other
    → アカウント作成
= form_for(resource, as: resource_name, url: session_path(resource_name), html: { id: "login-form" }) do |f|
  .field
    = f.email_field :email, placeholder: "Email", autofocus: true, autocomplete: "email", id: "login-email"
  .field
    = f.password_field :password, placeholder: "Password", autocomplete: "current-password", id: "login-password"
  .actions
    = f.submit "ログイン", class: "submit-btn"