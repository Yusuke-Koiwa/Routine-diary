.routine-list
  - @routines.each do |routine|
    .routine
      .routine__text-box
        .routine__text-box__content
          = icon('fa', "#{routine.category.icon}")
          = routine.content
        .routine__text-box__start-date
          = "#{routine.created_at.strftime('%Y/%m/%d')} 開始"